Install express and json-server packages before initialization.
To load the page first connect with the json-server at port 3000 with the command "json-server --watch db.json --port 3000"
Then the website will be hosted locally @ localhost:3000
The server address is provided in the scripts and the data are stored in the db.json file once the server connection is established.
